{
  "name": "kuda",
  "version": "0.1.0",
  "private": true,
  "description": "乱数の管 — Cloudflare Worker entropy pool (ANU QRNG API版)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^7.0.1-rc",
    "wrangler": "^3.99.0"
  }
}
